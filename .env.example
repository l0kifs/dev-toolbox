# For a globally installed tool, copy this to ~/.dev_toolbox/.env and fill in.
# A .env in the current directory overrides it per-project; real env vars beat both.

# Required by `dev-toolbox ai-commit` — MiniMax API key used to generate commit messages.
MINIMAX_API_KEY=
